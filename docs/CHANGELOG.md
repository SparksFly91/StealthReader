# 更新日志

本文件记录 StealthReader 各版本的更新内容。

---

## v0.3.3（2026-09-03）

### ✨ 新增功能

- **意见反馈**：设置页新增「意见反馈」子页面，支持填写反馈人邮箱与富文本反馈内容（含粘贴/拖拽截图），通过 163 SMTP 以邮件形式发送至维护者邮箱，回复时自动回信至反馈人（Reply-To）。

### 🚀 工程

- 后端新增 `feedback_send` 命令与 `send_feedback` SMTP 服务（lettre + multipart/related 内联图片），SMTP 授权码存放于应用配置目录的 `feedback.config.json`（首次运行自动生成模板，不随代码提交）。
- 前端接入 Tiptap 富文本编辑器与 DOMPurify 过滤，新增 `feedback.ts` API 封装。

---

## v0.3.2（2026-08-25）
### 🎉 新版本发布！

### ✨ 新增功能

- **深色主题支持**：新增全局深色主题样式变量，适配明暗两种主题；配置 naive-ui 主题跟随系统或用户设置切换，并修复外观设置页面主题同步逻辑。
- **可视化更新弹窗**：新增 `UpdateModal` 更新弹窗组件，支持 Markdown 格式更新说明与下载进度展示；重构 updater 工具，拆分检查与更新流程，新增进度状态管理，更新流程交由弹窗组件统一处理。

### 🎨 优化

- 调整主布局样式，使用 CSS 变量适配主题切换与标题栏高度。
- 统一标题栏圆角变量使用（`radius-sm`），优化卡片间距等 UI 细节。

### 🚀 工程

- 新增 `tauri-plugin-prevent-default` 插件，区分开发/生产模式拦截默认事件。
- 优化发布流程：release 工作流自动从 CHANGELOG.md 提取当前 tag 对应版本更新日志作为 Release body。

---

## v0.3.0（2026-08-24 16:52）
### 🎉 新版本发布！

### ✨ 新增功能

- **应用自动更新**：接入 `@tauri-apps/plugin-updater` 与 `process` 插件，应用启动时静默检查并自动更新，完善了更新流程的异常处理与用户交互。
- **手动更新入口**：在「设置 → 关于」页面新增「检查更新」按钮，支持更新中/已是最新/更新失败等状态提示。

### 🎨 优化

- **书籍详情页**：重构布局，新增「继续阅读」快速入口；调整简介展示逻辑，优化页面排版。
- **阅读器**：优化章节标题显示逻辑。
- **关于页面**：重构并完善（升级为完整更新入口页面）。

### 📄 文档

- 新增界面预览图并补充 README，直观展示应用效果。

### 🔧 其他

- 升级依赖（updater/process 插件）并调整更新相关权限与配置（`capabilities`、`tauri.conf.json`）。

---

## v0.2.0（2026-08-24 14:00）
### 🎉 新版本发布！

### ✨ 新增功能

- **小说阅读器**：整页窗口可拖动；正文按字符串自动分页（一屏一页），支持点击左/右半屏、键盘方向键翻页；提供「上一章 / 下一章」快速切换。
- **阅读进度追踪**：新增 `reading_progress` 表记录每章阅读状态；章节列表已读章节置灰显示（`is_read`）；记录书籍最后阅读位置与时间。
- **书籍导入与解析**：自动识别文件编码（GBK/UTF-8/Big5 等）并解码，按正则智能解析章节，事务一次性写入书籍与全部章节。
- **章节标题模糊搜索**：详情页支持按章节标题关键字搜索。
- **全局加载状态**：新增 `useLoading` hook 与全局加载遮罩，可自定义加载文字（导入中…/加载中…/删除中…）。
- **书籍管理**：列表右键「编辑 / 删除」，编辑弹窗、删除二次确认，默认封面图 `cover.jpg`。

### 🎨 优化

- 详情页章节列表双列展示、分页组件；简介区域滚动条包裹；返回按钮悬浮化。
- 阅读器默认封面、返回按钮样式与阅读字号优化。
- 数据库模型与表结构对齐（`introduction` 字段、`chapters` 主键、可空 `last_read_time`）。

### 🐛 修复

- 修复 Windows 下 SQLite 路径无法打开数据库的问题。
- 修复重复阅读已读章节时的 `UNIQUE constraint failed`（改为 UPSERT）。
- 修复删除确认无响应、Tauri 命令参数 camelCase 不匹配、`NaiveDateTime` 序列化失败等问题。

### 🚀 工程

- 配置 GitHub Actions 多平台打包与 release 发布流程（Windows/macOS/Linux）。
