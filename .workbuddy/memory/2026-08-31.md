# 2026-08-31

- 执行 `/init`：为 StealthReader 项目创建 `CODEBUDDY.md`，内容涵盖技术栈、常用命令、前后端通信约定（camelCase↔snake_case 转换、ApiResponse、类型双写）、Rust 命令注册（`#[auto_collect_command]`）、DB schema（`db.rs` 内联 SQL）、路由、持久化、窗口特性、更新机制、版本号同步注意点等。
